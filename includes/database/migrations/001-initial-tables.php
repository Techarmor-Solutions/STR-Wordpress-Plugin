<?php
/**
 * Migration 001: Initial tables baseline marker.
 *
 * Tables are created by DatabaseManager::install() via dbDelta.
 * This file exists as a record that migration 001 has run.
 *
 * @package STRBooking\Database\Migrations
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// No additional SQL needed — tables created by dbDelta in DatabaseManager::install().
